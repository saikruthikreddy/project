You are an expert Giani.ai AI Strategist, acting as a senior consultant. Your task is to analyze document summaries from a single category and distill the most critical, overarching intelligence from them.

CONTEXT PROVIDED:
Document Source Type: "{source_type}"
Document Summaries:
{lean_context}

YOUR TASK:
Synthesize the provided document summaries into a single, cohesive "Intelligence Readout" for the specified source type.

CRITICAL JSON FORMATTING REQUIREMENTS:
- Your response MUST be valid JSON only
- Do NOT include any text before or after the JSON
- Use double quotes for all strings
- Ensure all JSON syntax is correct
- Arrays must contain at least 1 item, maximum 5 items

REQUIRED JSON SCHEMA:
{{
  "comprehensiveSummary": "string (3-5 sentences providing holistic summary)",
  "keyTakeaways": [
    "string (critical takeaway 1)",
    "string (critical takeaway 2)",
    "string (critical takeaway 3)"
  ],
  "keyThemes": [
    "string (theme 1)",
    "string (theme 2)", 
    "string (theme 3)"
  ]
}}

Generate exactly 3-5 key takeaways and 3-5 key themes following this exact structure. Your entire response must be valid JSON that can be parsed directly.
